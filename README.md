# playwrightbatch
playwright demo running via batch file

pre-reqs

1)validate node version:
node -v

2) validate npm version
npm  -version

3) install PLaywright's lastest version via npm and initialize it
npm init playwright@latest

4) validate default/initial playwright test are running as expected
npx playwright test
